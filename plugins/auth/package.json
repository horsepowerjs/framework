{
    "name": "@horsepower/auth",
    "version": "1.0.0",
    "description": "Authorization for the horsepower http server.",
    "main": "lib/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prepack": "rm -rf lib && rm -rf types && tsc -p ."
    },
    "types": "types/index.d.ts",
    "author": "",
    "dependencies": {
        "@horsepower/mysql": "^1.0.0",
        "@horsepower/router": "^1.0.7",
        "@horsepower/server": "^1.0.8",
        "@horsepower/session": "^1.0.5",
        "bcrypt": "^3.0.6"
    },
    "license": "ISC",
    "files": [
        "lib",
        "types"
    ],
    "devDependencies": {
        "@types/bcrypt": "^3.0.0"
    }
}