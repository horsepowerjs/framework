{
    "name": "@horsepower/mysql",
    "version": "1.0.1",
    "description": "Mysql addon for the horsepower http server.",
    "main": "lib/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prepack": "rm -rf lib && rm -rf types && tsc -p ."
    },
    "types": "types/index.d.ts",
    "author": "",
    "dependencies": {
        "@horsepower/server": "^1.0.0",
        "mysql": "^2.17.1"
    },
    "license": "ISC",
    "files": [
        "lib",
        "types"
    ],
    "devDependencies": {
        "@types/mysql": "^2.15.6"
    }
}
