{
    "name": "@horsepower/template",
    "version": "1.0.4",
    "description": "Template manager for the horsepower http server.",
    "main": "lib/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prepack": "rm -rf lib && rm -rf types && tsc -p ."
    },
    "types": "types/index.d.ts",
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@horsepower/storage": "^1.0.0",
        "html-minifier": "^4.0.0",
        "jsdom": "^15.1.1"
    },
    "devDependencies": {
        "@types/html-minifier": "^3.5.3",
        "@types/jsdom": "^11.12.0"
    },
    "files": [
        "lib",
        "types"
    ]
}
